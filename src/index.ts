export { createEnhancedQuill } from './core/createEnhancedQuill';

// Optional: export types if you want to expose custom extension interface
export type {
  QuillExtension,
  EnhancedQuillOptions,
  StyleRequest,
} from './core/types';